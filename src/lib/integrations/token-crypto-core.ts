import { createCipheriv, createDecipheriv, randomBytes } from "node:crypto";
function keyFromBase64(value:string){const key=Buffer.from(value,"base64");if(key.length!==32)throw new Error("OAUTH_TOKEN_ENCRYPTION_KEY must decode to exactly 32 bytes");return key;}
export function encryptSecretCore(plaintext:string,base64Key:string){const iv=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",keyFromBase64(base64Key),iv);const ciphertext=Buffer.concat([cipher.update(plaintext,"utf8"),cipher.final()]);const packed=Buffer.concat([Buffer.from([1]),iv,cipher.getAuthTag(),ciphertext]);return`\\x${packed.toString("hex")}`;}
export function decryptSecretCore(value:string,base64Key:string){const packed=Buffer.from(value.startsWith("\\x")?value.slice(2):value,"hex");if(packed[0]!==1||packed.length<30)throw new Error("Unsupported encrypted secret format");const decipher=createDecipheriv("aes-256-gcm",keyFromBase64(base64Key),packed.subarray(1,13));decipher.setAuthTag(packed.subarray(13,29));return Buffer.concat([decipher.update(packed.subarray(29)),decipher.final()]).toString("utf8");}
