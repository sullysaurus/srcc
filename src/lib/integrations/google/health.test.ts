import {describe,expect,it}from"vitest";import{classifyConversionOutcome,evaluateAdsHealth}from"./health";
describe("Google Ads health",()=>{it("keeps business outcomes distinct",()=>{expect(classifyConversionOutcome("Website inquiry")).toBe("inquiry");expect(classifyConversionOutcome("Retainer paid booking")).toBe("booked_event");expect(classifyConversionOutcome("Collected revenue")).toBe("revenue_collected")});it("flags inactive actions, spend without outcomes, and account-level enhanced conversion setup",()=>{const issues=evaluateAdsHealth([{providerId:"1",name:"Lead",category:null,status:"REMOVED",primaryForGoal:true,enhancedConversionsEnabled:false}],[{date:"2026-08-01",campaignId:"2",campaignName:"DSA",campaignStatus:"ENABLED",budgetCents:1000,impressions:1,clicks:1,costCents:5000,conversions:0,conversionValueCents:0,impressionShare:null}],[],{enhancedConversionsForLeadsEnabled:false,acceptedCustomerDataTerms:false});expect(issues.map(issue=>issue.key)).toEqual(expect.arrayContaining(["inactive_conversion_action","spend_without_conversions","enhanced_conversions_disabled","customer_data_terms_not_accepted"]))});it("flags enabled conversion actions with no recent activity",()=>{expect(evaluateAdsHealth([{providerId:"1",name:"Inquiry",category:null,status:"ENABLED",primaryForGoal:true,enhancedConversionsEnabled:true}],[],[]).map(issue=>issue.key)).toContain("no_recent_conversions")})});
