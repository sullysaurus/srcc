import { Sparkles } from "lucide-react";
import { signIn } from "./actions";
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="grid min-h-screen place-items-center bg-void p-5"><section className="w-full max-w-md rounded-2xl border border-white/10 bg-cream p-7 shadow-2xl"><span className="grid size-11 rotate-3 place-items-center rounded-xl bg-marigold text-ink shadow-[3px_3px_0_#ef725f]"><Sparkles className="size-5"/></span><h1 className="mt-6 font-display text-4xl tracking-[-.04em]">Welcome back.</h1><p className="mt-2 text-xs leading-5 text-ink/50">Sign in to the Southern Revelry operations workspace.</p>{error?<p role="alert" className="mt-4 rounded-lg border border-coral/35 bg-coral/[.08] px-3 py-2 text-[10px] font-bold text-coral">{error==="configure_supabase"?"Supabase must be configured before live sign-in.":"Email or password was not accepted."}</p>:null}<form action={signIn} className="mt-6 space-y-4"><label className="grid gap-2 text-[10px] font-bold">Email<input required name="email" type="email" autoComplete="email" className="h-11 rounded-lg border bg-panel px-3 text-sm"/></label><label className="grid gap-2 text-[10px] font-bold">Password<input required name="password" type="password" autoComplete="current-password" minLength={8} className="h-11 rounded-lg border bg-panel px-3 text-sm"/></label><button className="h-11 w-full rounded-lg bg-void text-xs font-bold text-white">Sign in securely</button></form></section></main>}
